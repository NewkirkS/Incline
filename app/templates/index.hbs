{{#if session.isAuthenticated}}
<div class="row">
  <div class="col-md-8 col-md-offset-2">
    {{new-habit newHabit="newHabit"}}
    {{habit-list model=model addInstance="addInstance"}}
  </div>
</div>
{{else}}
<div class="landing-header">
  <h1 class="landing-header-title">INCLINE</h1>
</div>
{{landing-page signOut="signOut" signIn="signIn"}}
{{/if}}
